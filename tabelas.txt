Cartas:

    id (varchar (255))
    base
    descrição


Territórios:

    id (int ou bigint)
    descrição (varchar (255))
    cidade (varchar (255))
    ponto de referência (varchar (255))
    estado (varchar (255))

Entrega de Cartas

    id (int ou bigint)
    id da Cartas (varchar (255))
    id do Territórios (int ou bigint)
    data da Entrega (date)
    local (varchar (255))

Pessoas

    id (int ou bigint)
    nome (varchar (255))
    contato (varchar (255))

Conversas 

    id (int ou bigint) required
    id da Pessoas (int ou bigint)
    id do territorio (int ou bigint)
    data da Conversa date

Revista

    id (varchar 255 ) required
    descricacao (varchar 255 ) required
    ano int

  Livro

    id (varchar 255 ) required
    descricacao (varchar 255 ) required
    ano int  
    Quantidade de capítulos int

   Folhetos

    id
    descricao
    ano
    tamanho



